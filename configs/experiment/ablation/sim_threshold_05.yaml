# @package _global_
# ABLATION: High similarity threshold (min 0.5)
# Tests retrieval quality - what if we only use very similar templates?

task_name: "ablation_sim05"
tags: ["ablation", "similarity-threshold", "sim05"]
seed: 42

model:
  min_template_similarity: 0.5
