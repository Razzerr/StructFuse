# @package _global_
# ABLATION: Vary number of templates (k=8)
# Tests if more templates = better performance

task_name: "ablation_k8"
tags: ["ablation", "k-templates", "k8"]
seed: 42

model:
  topk: 8