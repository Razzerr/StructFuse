# @package _global_
# ABLATION (L1): frontier_8M MINUS Tversky regularizer (BCE-only loss).
# Defends the BCE+Tversky design choice. Architecture inherited from
# frontier_8M defaults; loss is the only delta.

task_name: "ablation_bce_only"
tags: ["ablation", "L1", "bce-only", "no-tversky"]
seed: 42

model:
  use_tversky: false
  tversky_weight: 0.0
