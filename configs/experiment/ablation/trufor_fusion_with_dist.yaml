# @package _global_
# Equal-input fusion comparison: TruFor-style cross-attention receives the
# same contact/count and 9-channel distance priors as frontier_8M.

task_name: "ablation_trufor_fusion_with_dist"
tags: ["ablation", "fusion-parity", "trufor", "tpl-dist", "8M"]
seed: 42

model:
  fusion_strategy: "trufor"
  use_tpl_dist_bins: true

data:
  compute_dist_bins: true
