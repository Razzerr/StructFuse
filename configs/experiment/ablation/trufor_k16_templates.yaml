# @package _global_
# TruFor+dist K-sweep: sixteen retrieved templates.
# PriorBuilder lives in ContactDataModule, so override data.topk and model.topk.

task_name: "ablation_trufor_k16"
tags: ["ablation", "trufor", "tpl-dist", "k16", "8M"]
seed: 42

data:
  topk: 16
  compute_dist_bins: true

model:
  fusion_strategy: "trufor"
  use_tpl_dist_bins: true
  topk: 16
