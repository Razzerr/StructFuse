name: structfuse

channels:
  - pytorch
  - conda-forge
  - defaults

# Install minimal conda dependencies for performance, then use pip for the rest
# This ensures compatibility with requirements.txt (single source of truth)

dependencies:
  - python=3.11
  - pip>=23
  
  # Install pip packages from requirements.txt
  - pip:
      - -r requirements.txt
